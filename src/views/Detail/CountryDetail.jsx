import { useState } from 'react';
import { useUser } from '../../context/UserContext'


export default function CountryForm({ onAddEntry }){

    return(
        <div>Enter Country Visited</div>
    )
}